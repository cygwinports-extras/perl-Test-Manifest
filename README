perl-Test-Manifest
------------------------------------------
Test::Manifest overrides the default behaviour by replacing the 
test_via_harness target in the Makefile. Instead of running at the t/*.t 
files in ascii-betical order, it looks in the t/test_manifest file to 
find out which tests you want to run and the order in which you want to 
run them. It constructs the right value for MakeMaker to do the right 
thing.

Runtime requirements:
  perl-5.10.0-5

Canonical website:
  http://search.cpan.org/dist/Test-Manifest/

Canonical download:
  http://search.cpan.org/CPAN/authors/id/B/BD/BDFOY/

-------------------------------------------

Build instructions:
  unpack perl-Test-Manifest-1.22-X-src.tar.bz2
    if you use setup to install this src package, it will be
	 unpacked under /usr/src automatically
  cd /usr/src
  cygport ./perl-Test-Manifest-1.22-X.cygport all

This will create:
  /usr/src/perl-Test-Manifest-1.22-X-src.tar.bz2
  /usr/src/perl-Test-Manifest-1.22-X.tar.bz2

-------------------------------------------

Files included in the binary package:

  /usr/lib/perl5/vendor_perl/5.10/Test/Manifest.pm
  /usr/lib/perl5/vendor_perl/5.10/i686-cygwin/auto/Test/Manifest/.packlist
  /usr/share/doc/perl-Test-Manifest-1.22/Changes
  /usr/share/doc/perl-Test-Manifest-1.22/LICENSE
  /usr/share/doc/perl-Test-Manifest-1.22/README
  /usr/share/man/man3/Test.Manifest.3.gz

------------------

Port Notes:

----- version 1.22-1 -----
Initial release for Cygwin.


Cygwin port maintained by:
http://sourceware.org/cygwinports/
